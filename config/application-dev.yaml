# ========================================
# Dev 环境配置 - 开发服务器
# ========================================

spring:
  application:
    name: catBot-dev
  data:
    redis:
      host: ${DEV_SERVER_HOST}
      port: 6379
    mongodb:
      host: ${DEV_SERVER_HOST}
      port: 27017
      database: botDB
      username: root
      password: ${DB_PASSWORD}
      authentication-database: admin

ai:
  model:
    gemini:
      api-key: ${GEMINI_API_KEY}
    qwen:
      api-key: ${QWEN_API_KEY}
    ollama:
      url: http://${DEV_SERVER_HOST}:11434
      temperature: 0.5
  rag:
    max-results: 5
    min-score: 0.6
  service:
    base-url: http://${DEV_SERVER_HOST}:8081
    timeout: 30000

train:
  model:
    cache-enabled: true
    cache-dir: ./model-cache
    max-models: 10

milvus:
  host: ${DEV_SERVER_HOST}
  port: 19530
  collection-name: message_embeddings_dev
  enable: true

neo4j:
  uri: bolt://${DEV_SERVER_HOST}:7687
  username: neo4j
  password: ${DB_PASSWORD}

onebot:
  api:
    base-url: http://${DEV_SERVER_HOST}:3000
    timeout: 5000
    retry:
      max-attempts: 3
      delay-ms: 1000
      multiplier: 2.0

bot:
  framework:
    permission:
      enabled: true
  log:
    level: INFO

logging:
  level:
    root: INFO
    com.shuanglin: DEBUG
